# Text

A plain paragraph card — narrative context between charts.

## Using it on a page

Add via the editor's **+ Text** button, then edit the copy in the config drawer.

## Demo wiring

The `/library` preview renders the same card styling PageGrid uses.
